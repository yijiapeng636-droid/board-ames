## Agent skills

### Issue tracker

任务、规格和问题统一记录在 GitHub Issues 中，通过 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

分诊使用五个默认标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单上下文领域文档结构。详见 `docs/agents/domain.md`。
